# Домашнее задание к работе 11

## Условие задачи
Сумма модулей элементов массива, расположенных после первого
отрицательного элемента.
## 1. Алгоритм и блок-схема

### Алгоритм

##### 1 Начало

2 Объявить переменные для ввода пользователя:

      n, i - входные данные

3 Задать исходные данные:

      n - размер массива, не превышающий 100

      i - входящие элементы массива
      
      sum - сумма модулей

      otr = -1 - флаг, отвечающий за нахождения отрицательного элемента

      #define MAX_SIZE 100 - константа
      
4 Начать цикл с перебором элемента массива:

      for (i = 0; i < n; i++)
      {
      scanf("%lf", &arr[i])

5 Проверить условие на нахождение отрицательного элемента и "поднять" флажок:

     if (arr[i] < 0 && otr == -1)
    {
    otr = i;
    }

6 Рассмотреть случай, когда отрицательный элемент не был найден и завершить выполнение программы:

    if (otr == -1)
    {
    printf("Отрицательных элементов нет");
    return 0;
    }
  
7 Если же все же первый отрицательный элемент был найдет, то начать цикл после него и суммировать числа с модулем:
  for (i = otr + 1; i < n; i++)
  {
    sum = sum + fabs(arr[i]);
  }

8 Вывести результат:

    printf("Сумма модулей элементов после первого отрицательного: %lf", sum);

##### 9 Конец


### Блок-схема

<img width="135" height="1001" alt="Диаграмма без названия drawio(1)" src="https://github.com/user-attachments/assets/0acc1540-d5b8-4922-93a7-874e42f1cb2b" />


## 2. Реализация программы

#include <stdio.h>

#include <math.h>

#include <locale.h>

int main()

{

    setlocale(LC_ALL, "RUS");
    
    double h, A, k, Dlina, v_end, S_full, S_horizontal;
    
    const double g = 9.81;

    printf("Введите высоту горки h в метрах: ");
    
    scanf("%lf", &h);

    printf("Введите угол наклона A(рад): ");
    
    scanf("%lf", &A);

    printf("Введите коэффициент трения k: ");
    
    scanf("%lf", &k);   

    Dlina = h / sin(A);
    
    double a = g * (sin(A) - k * cos(A));
    
    v_end = sqrt(2 * a * Dlina);
    
    S_horizontal = (v_end * v_end) / (2 * k * g);
    
    S_full = Dlina + S_horizontal;

    printf("\nРезультат\n");
    
    printf("Общий путь(S), который проедут санки равен: %.2f\n", S_full);

    system("pause");
    
}

## 3. Результаты работы программы

Введите высоту горки h в метрах: 10

Введите угол наклона A(рад): 1

Введите коэффициент трения k: 1

Результат

Общий путь(S), который проедут санки равен: 15,46

Для продолжения нажмите любую клавишу . . .

## 4. Информация о разработчике

Крылов Олег бИЦ-252
